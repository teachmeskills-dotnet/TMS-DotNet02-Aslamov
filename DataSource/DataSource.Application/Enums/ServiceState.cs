namespace DataSource.Application.Enums
{
    /// <summary>
    /// Service state (stopped / working). 
    /// </summary>
    public enum ServiceState
    {
        Stopped = 0,
        Working = 1,
    }
}